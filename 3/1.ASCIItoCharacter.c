#include <stdio.h>

int main() {
    int ascii_value = 65; // Change this to the ASCII value you want to convert
    char character = (char)ascii_value;
    printf("The character for ASCII value %d is: %c\n", ascii_value, character);
    return 0;
}
